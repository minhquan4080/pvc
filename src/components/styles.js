'use strict';

import {
    StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    margin: 0,
    padding: 0
  },
  defaultTextIput: {
    height: 35,
    borderColor: 'white',
    borderWidth: 1,
    padding: 5,
    backgroundColor: '#FFFFFF50',
    color: '#FFFFFF'
  }
});

module.exports = styles;
